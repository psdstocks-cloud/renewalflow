# Railway/Nixpacks configuration for RenewalFlow backend
# This tells Railway to build and run the server from the server/ directory

# Nixpacks will auto-detect Node.js version from .nvmrc or package.json
# If you need to specify explicitly, use a version available in nixpkgs
[phases.setup]
# nixPkgs = ["nodejs"]  # Let it auto-detect

[phases.install]
cmds = [
  "cd server && npm ci",
  "cd server && npx prisma generate"
]

[phases.build]
cmds = [
  "cd server && npm run build",
  "cd server && npx prisma migrate deploy"
]

[start]
cmd = "cd server && npm start"

