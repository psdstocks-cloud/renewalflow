{
  "$schema": "https://railway.app/railway.schema.json",
  "$comment": "This service uses Dockerfile for backend deployment",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

